log_level = "warn"
outputs = "All"
position = "Top"
app_launcher_cmd = "walker"

# Colores Principales Dracula
primary_color = "#bd93f9"   # Purple (El color firma de Dracula)
success_color = "#50fa7b"   # Green
text_color = "#f8f8f2"      # Foreground (Blanco cremoso)

# Configuración de Workspaces
workspace_colors = [ "#bd93f9", "#ff79c6" ]         # Purple y Pink
special_workspace_colors = [ "#8be9fd", "#50fa7b" ] # Cyan y Green

[appearance.background_color]
base = "#282a36"   # Background (Oscuro principal)
weak = "#44475a"   # Current Line/Selection (Gris azulado)
strong = "#6272a4" # Comment (Azul grisáceo más claro)

[appearance.secondary_color]
base = "#191a21"   # Background más oscuro para contraste

[appearance.danger_color]
base = "#ff5555"   # Red
weak = "#ffb86c"   # Orange

[modules]
left = [ [ "Workspaces" ] ]
center = [ "WindowTitle" ]
right = [ "SystemInfo", [  "Tray", "Clock", "Privacy", "Settings" ] ]

[workspaces]
enable_workspace_filling = true

[window_title]
truncate_title_after_length = 100

[settings]
lock_cmd = "playerctl --all-players pause; hyprlock &"
audio_sinks_more_cmd = "pavucontrol -t 3"
audio_sources_more_cmd = "pavucontrol -t 4"
wifi_more_cmd = "nm-connection-editor"
vpn_more_cmd = "nm-connection-editor"
bluetooth_more_cmd = "blueman-manager"

[clock]
# Formato de 24h con fecha corta
format = "%H:%M - %a %d"

[appearance]
# ashell pide "DejaVu Sans" por omision y no esta instalada aqui. Al no
# resolverla hacia respaldo glifo a glifo con metricas inconsistentes, y
# "62 °C" salia como "6 2 °C". Fijada queda coherente con GTK y Hyprland.
font_name = "Cantarell"
style = "Islands"
