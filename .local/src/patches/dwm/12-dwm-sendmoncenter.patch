diff --color -u a/dwm.c b/dwm.c
--- a/dwm.c	2021-08-19 01:08:15.987985747 +0200
+++ b/dwm.c	2021-08-19 01:08:41.294601661 +0200
@@ -1679,6 +1679,8 @@
 	detachstack(c);
 	c->mon = m;
 	c->tags = m->tagset[m->seltags]; /* assign tags of target monitor */
+    c->x = c->mon->mx + (c->mon->mw - WIDTH(c)) / 2;
+    c->y = c->mon->my + (c->mon->mh - HEIGHT(c)) / 2;
 	attachaside(c);
 	attachstack(c);
 	focus(NULL);
